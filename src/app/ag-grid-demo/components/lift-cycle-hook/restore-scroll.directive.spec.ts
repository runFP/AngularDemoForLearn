import { RestoreScrollDirective } from './restore-scroll.directive';

describe('RestoreScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RestoreScrollDirective();
    expect(directive).toBeTruthy();
  });
});
